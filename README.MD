```BASH
docker-compose up -d
```

```BASH
Host: localhost
Port: 3306
User: testuser (as specified in MYSQL_USER)
Password: testpassword (as specified in MYSQL_PASSWORD)
Database: testdb (optional)
```